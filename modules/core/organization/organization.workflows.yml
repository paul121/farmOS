organization_default:
  id: organization_default
  group: organization
  label: 'Default'
  states:
    active:
      label: Active
    archived:
      label: Archived
  transitions:
    archive:
      label: 'Archive'
      from: [active]
      to: archived
    to_active:
      label: 'Make active'
      from: [archived]
      to: active
