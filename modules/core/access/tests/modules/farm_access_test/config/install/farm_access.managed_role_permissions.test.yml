# Simple test permissions to add to managed roles.
id: test
default_permissions:
  - test default permission
config_permissions:
  - test config access permission
permission_callbacks:
  - Drupal\farm_access_test\CustomTestPermissions::permissions
